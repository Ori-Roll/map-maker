// precipitation map
/* 
function makePrecipitationMap(canvasSize) {
	let pointsDistance = 5;
	const pMap = [];
	let xPos = 0;
	let yPos = 0;
	for (yPos; yPos < canvasSize.height / pointsDistance; yPos += pointsDistance) {
		for (xPos; xPos < canvasSize.width / pointsDistance; xPos += pointsDistance) {
			console.log("x = " + xPos + " y = " + yPos);
		}
	}
} */
makePrecipitationMap();
